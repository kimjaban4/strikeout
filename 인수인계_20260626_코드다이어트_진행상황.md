# 마운드 심리전 코드 다이어트 인수인계서

작성일: 2026-06-26
작업 위치: `M:\gamedev\baseball\야구게임`
현재 목적: 기능을 유지하면서 불필요한 파일, 예전 UI 잔재, 중복 코드를 단계적으로 줄인다.

## 0. 작업 전 백업

전체 작업 전 백업을 먼저 생성했다.

```text
M:\gamedev\baseball\backups\baseball-full-before-diet-20260626-180348.tar
크기: 477,001,728 bytes
```

백업 제외:

```text
.git
node_modules
```

이유:

```text
.git은 원격 저장소와 로컬 히스토리로 복구 가능
node_modules는 package-lock.json 기준으로 재설치 가능
```

## 1. 직전까지 완료된 모바일 UI 패치

최근 완료 커밋:

```text
b1e246d Separate mobile toast slots
0fd91e8 Fix mobile log suspicion layout
5080059 Refine mobile feedback and reward flow
26bf1a5 Fix mobile reward and duel feedback
b3f08ae Refine mobile records and tag feedback
f4464a2 Fix mobile course and reward feedback
a10860a Refine mobile pitch log reactions
8e4b7bf Compact mobile pitch record panel
```

주요 반영 내용:

```text
의심도 UI를 승부기록 위 별도 카드로 분리
승부기록과 의심도 UI가 서로 크기를 밀지 않도록 고정 레이아웃 적용
승부기록 내부만 스크롤되게 변경
모바일 토스트 슬롯 분리
결과/보상/성장/NEXT BATTER 토스트는 상단 슬롯
스트라이크/볼/스윙 판정 토스트는 하단 슬롯
성장 토스트와 타이밍 토스트 겹침 제거
성장 토스트는 성장+와 실제 효과명을 시각적으로 분리
체크스윙 토스트 제거
타이밍이 맞아갑니다, 타이밍이 어긋났습니다 같은 애매한 문구 제거
구종 등급 상승 시 구종 버튼 색상이 등급에 맞게 반영
미션 UI 클릭 시 상세 설명 팝업 표시
성장/진화 보상은 확인 버튼 없이 카드 클릭 즉시 적용 흐름으로 변경
```

검증 기록:

```text
npm.cmd run test:smoke 통과
npm.cmd run check:structure 통과
모바일 390x844 브라우저 실측 확인
```

## 2. 1차 코드 다이어트: 로컬 산출물 정리

목적:

```text
코드 리스크 없이 작업 폴더를 가볍게 만든다.
Git에 올라가지 않는 검증 이미지와 빌드 산출물을 제거한다.
```

삭제한 항목:

```text
backup/
_site/
_site-test/
test-results/
mobile-*.png
text-layer-*.png
pitcher-select-*.png
release-sync-*.png
*.log
```

남긴 항목:

```text
코드 파일
기획 문서
assets
balance-reports
node_modules
.git
```

결과:

```text
Git 추적 파일 변경 없음
커밋할 내용 없음
```

주의:

```text
삭제 전 전체 백업 tar가 있으므로 필요 시 복구 가능
```

## 3. 2차 코드 다이어트: 보상 확인 버튼 잔재 제거

목적:

```text
현재 보상 UI는 카드 클릭 즉시 적용 방식이다.
예전 "카드 선택 후 확인 버튼" 흐름의 잔재를 제거한다.
```

제거한 것:

```text
rewardConfirmButton DOM 캐시
index.html의 rewardConfirmButton 버튼
selectedRewardIndex 상태값
selectedRewardIndex 초기화 코드
confirmRewardChoice 함수
rewardConfirmButton 클릭 이벤트 바인딩
MP.debug.confirmRewardChoice 노출
core-evolution-card.is-selected 스타일
core-evo-check 스타일
reward-confirm-button 스타일
보상 선택 중 확인 버튼 pointer-events 차단 CSS
```

유지한 것:

```text
보상 카드 클릭 즉시 applyReward(index) 실행
보상 reveal 애니메이션 중 클릭 방지
rewardChoiceList 클릭 이벤트 위임
core-evolution-card 공통 카드 스타일
모바일 카드/구종/존 선택용 is-selected 클래스
```

변경 파일:

```text
game.js
js/01-state.js
js/02-game-core.js
index.html
styles.css
```

라인 변화:

```text
5 files changed
3 insertions
122 deletions
```

중요:

```text
game.js가 원본이고 js/ 폴더는 tools/split-game-modules.mjs로 재생성된다.
따라서 game.js 수정 후 node tools\split-game-modules.mjs를 실행했다.
```

## 4. 2차 검증 결과

실행한 검사:

```text
rg -n "selectedRewardIndex|rewardConfirmButton|confirmRewardChoice|core-evo-check|core-evolution-card\.is-selected|reward-confirm-button" game.js js index.html styles.css css tests tools
node --check game.js
node --check js\02-game-core.js
npm.cmd run check:structure
npm.cmd run test:smoke
```

결과:

```text
보상 확인 버튼 잔재 검색 결과 없음
game.js 문법 검사 통과
js\02-game-core.js 문법 검사 통과
프로젝트 구조 검사 통과
스모크 테스트 8개 통과
```

브라우저 확인:

```text
모바일 390x844
rewardOverlay 표시 확인
rewardConfirmButton 미존재 확인
reward-choice-card 3개 표시 확인
카드 클릭 후 rewardOverlay 닫힘 확인
```

브라우저 확인 결과:

```json
{
  "before": {
    "overlayVisible": true,
    "confirmExists": false,
    "cardCount": 3
  },
  "after": {
    "overlayHidden": true
  }
}
```

## 5. 현재 남은 다이어트 후보

### 5-1. useCardUiV2 레거시 분기 제거

후보:

```text
state.useCardUiV2
state.useCardUiV2 !== false 분기
legacyExtra 렌더링 분기
legacy-ui 관련 assert skip
```

판단:

```text
현재 카드 UI가 V2 고정이라면 제거 가능성이 높다.
단, 투수/타자 카드 상세 표시와 태그 설명 위치에 직접 영향이 있으므로 모바일 브라우저 검증 필수.
```

권장 순서:

```text
1. useCardUiV2 참조 검색
2. false 분기 제거
3. V2 렌더링을 기본값으로 고정
4. split-game-modules 실행
5. smoke test
6. 모바일에서 투수/타자 카드 펼침, 핵심/보조 태그 설명 위치 확인
```

### 5-2. CSS 영역별 중복 정리

후보:

```text
styles.css
css/31-mobile-reference.css
카드/보상/토스트/모바일 기록 패널 관련 override 누적
```

판단:

```text
한 번에 통합하면 위험하다.
특정 영역 하나씩 줄여야 한다.
```

권장 순서:

```text
보상 카드 CSS
토스트 CSS
승부기록 CSS
모바일 선수 카드 CSS
투구 버튼 CSS
```

### 5-3. test.html 정리 여부 판단

후보:

```text
test.html
```

판단:

```text
별도 디버그 콘솔 성격으로 보인다.
현재 메인 실행은 index.html이다.
실제로 쓰지 않으면 제거 가능하다.
```

주의:

```text
test.html이 수동 디버그에 쓰이고 있다면 남겨야 한다.
```

### 5-4. styles.layered.backup.css 정리 여부 판단

후보:

```text
styles.layered.backup.css
tools/consolidate-css.mjs
```

판단:

```text
styles.layered.backup.css는 tools/consolidate-css.mjs의 기본 입력값이다.
파일만 지우면 도구가 깨진다.
삭제하려면 도구 기본값도 함께 바꾸거나 도구 자체를 제거해야 한다.
```

### 5-5. game.js와 js/ 중복 구조

현재 구조:

```text
game.js: 원본
js/00-constants.js
js/01-state.js
js/02-game-core.js
js/03-pitch-progression.js
js/04-stage-theme.js
```

판단:

```text
중복처럼 보이지만 빌드/분리 구조다.
당장 삭제 대상이 아니다.
구조를 바꾸려면 배포와 테스트 파이프라인까지 같이 수정해야 한다.
```

## 6. 작업 시 반드시 지킬 것

```text
game.js 수정 후 node tools\split-game-modules.mjs 실행
js/만 직접 고치지 말 것
모바일 UI 변경은 390x844 또는 360x740 브라우저 실측 확인
기능 삭제 전 rg로 참조 확인
삭제 후 rg로 잔재 확인
테스트 최소 세트는 node --check game.js, node --check js\02-game-core.js, npm.cmd run check:structure, npm.cmd run test:smoke
```

## 7. 다음 작업 추천

가장 안전한 3차:

```text
useCardUiV2 레거시 분기 제거
```

이유:

```text
기능 방향이 이미 V2 카드 UI로 고정되어 있다.
남은 false 분기는 실제 사용자 플로우보다 예전 호환성 잔재에 가깝다.
삭제 범위가 검색으로 추적 가능하다.
테스트와 모바일 카드 확인으로 검증 가능하다.
```

대신 보류할 것:

```text
game.js/js 구조 개편
CSS 전체 통합
MP.debug 대량 삭제
```

보류 이유:

```text
한 번에 줄이는 양은 커 보이지만 테스트/시뮬/배포 흐름을 깨기 쉽다.
현재 목적에는 단계별 삭제가 더 안전하다.
```
